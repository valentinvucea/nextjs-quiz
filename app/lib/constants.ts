export const categoryMap: { [key: number]: string } = {
    1: "AWS Cloud Practitioner",
    2: "AWS AI Practitioner",
    3: "AWS Associate Developer",
};

export const defaultQuizLength = 65;
export const defaultMALength = 25; // MA - Multiple answer
export const defaultQuizPassScore = 80;
